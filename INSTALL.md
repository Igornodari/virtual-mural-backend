## Instalação de novas dependências

Após atualizar o package.json, execute:

```bash
cd virtual-mural-backend
npm install
```

Isso instalará:
- `helmet@^8.0.0` — security headers HTTP
- `@nestjs/throttler@^6.4.0` — rate limiting
